# Implementation Notes: agentbook_thesis

Running log of anything the spec/plan did NOT already settle. Append as you go.

| Task | Type | Note | Rationale |
|------|------|------|-----------|
| (gate) | decision | Checklist gate: validated all 14 CHK items against the spec and marked them `[x]`, rather than stopping for the user to tick boxes I'd already verified during spec + analyze. | All items genuinely pass (0 clarifications, FRs testable, SCs measurable, stories independently testable); manual ticking would be bureaucratic. |
| (env) | deviation | `make test` targets a conda env (`./env`) that doesn't exist; the repo actually uses `.venv` (uv). Using `.venv/bin/python -m pytest` for the test baseline. | Don't block on a Makefile/conda mismatch unrelated to this feature. |
| T004 | unknown | `gepa` and `SkillOpt` are not installed in `.venv`; `SkillOpt` is a Microsoft Research repo, likely git-install not PyPI. SkillOpt→`Optimizer` conformance spike (A-004) cannot run until it's installed. | Recorded as an open risk; US3 stays gated on this. See research addendum. |
| T013/T031 | decision | Implemented the `BudgetExhausted` catch + partial-halt directly in `loop.py` at foundational time (T031 is nominally a US2 task). | The catch is intrinsic to the one written-once driver; splitting it across phases would mean shipping `loop.py` with a known unhandled path. |
| T012/T030 | decision | Added the host-facing outcome accessors (`best_candidate`/`best_score`/`frontier_snapshot`/`latest_diff`) to `session.py` now (T030 is nominally US2). | They are plain reads of session state with no external deps; cheaper to ship with the entity than to bolt on later. |
| T022/T032/T043 | decision | Wrote `tests/test_loop.py` with a `FakeOptimizer` to validate the driver deterministically — covering the no-kernel halves of T022 (stable PID, load-once, FR-004), T032 (clean partial halt), and T043 (no-self-rewrite, Protocol conformance). | Constitution IV forbids shipping `loop.py` untested; these assertions need no `gepa`/kernel, so they belong with the foundational code. The live-kernel halves remain in the US1/US3 notebook tasks. |
| T012 | deviation | `latest_diff` originally indexed `candidates[parent_id]`; fixed to look up parent by `candidate_id` since gate-rejected candidates are removed, breaking the id==index assumption. | Latent correctness bug caught in self-review. |
| T021 | decision | The runt kernel uses `env_source=uv:pyproject`, so `manage_dependencies` rewrote the repo `pyproject.toml`, promoting gepa/litellm/pandas/python-dotenv into **main** deps. Reverted: core deps back to matplotlib/numpy/ruff; gepa demo deps (gepa, litellm, boto3, pandas, datasets, python-dotenv) moved to the `[gepa]` optional extra. The substrate core imports none of them. | Constitution: substrate must stay optimizer-agnostic; gepa must not be a hard dep of agentbook. |
| T021 | deviation | litellm's Bedrock provider needs `boto3`/`botocore`; `gepa.examples.aime` needs HuggingFace `datasets`. Installed both into the live kernel via `uv pip` (no kernel restart, PID preserved) rather than `manage_dependencies` (which forces a restart + more pyproject churn). | Keep the kernel warm (SC-001) and pyproject clean. |
| T021 | unknown | Real run outcome: GEPA evolved 4 candidates but `best_idx=0` — no evolved system_prompt beat the seed on the 4-problem AIME valset (all valset scores 0.0; haiku can't solve these). Also `total_metric_calls=34` overshot the `max_metric_calls=30` cap by 4. | Honest real data: the substrate ran the full loop correctly; AIME-with-haiku is just very hard. GEPA's internal cap is soft (overshoots), which is why our uniform Budget guard exists. |
| T040 | decision | SkillOpt integrated in **engine mode via subprocess** to its own `scripts/train.py`, then mapped the on-disk run dir → Session (`sync_from_disk`). Its reflect/edit/gate are internal submodules and `ReflACTTrainer` is the entry, so driver-mode wiring would be fragile; the subprocess reuses SkillOpt's config machinery + backends unchanged. Adapter imports only stdlib (no `skillopt`), so it's importable/testable without the lib. | Robustness + honest "batch trainer, on-disk state" framing (research Decision 3 / SC-003 second state shape). |
| T040 | deviation | SkillOpt's `claude_chat` backend passes `--thinking <low/medium/...>`, but the installed `claude` CLI only accepts `enabled/adaptive/disabled` → every rollout errored (0 calls). Fixed by `model.reasoning_effort=""`, which drops the `--thinking` flag (claude_backend.py:256 `if effort:`). Robust long-term fix (noted, not built): a custom SkillOpt Bedrock backend reusing evaltools' `pydantic_ai.BedrockConverseModel` pattern, per user suggestion. | CLI-shelling backend is version-brittle; the SDK path (pydantic-ai/litellm) is stable. |
| T041 | decision | Per user steer ("use logs not reruns"), US3 reframed to **one minimal run → log-EDA**, not a 30-min training. SkillOpt's reflect already reads `predictions/<id>/conversation.json` from disk, so the log is the rollout↔reflect interface — the notebook's value is parsing it (`load_results`/`load_trajectories`), not running the loop. | SkillOpt is medium notebook-fit (batch + on-disk); honest framing is the GEPA contrast case. |
| T041 | deviation | `searchqa_id_split` ships **IDs only** (`{id}`); the rollout needs `{id,question,context,answers}`. Built a real split from **SQuAD** (`rajpurkar/squad`) in SearchQA format at `/tmp/abook_skillopt_split`. | Real data (Constitution I); the recovered notebook-06 used the same SQuAD-via-process_one pattern. |
| T042 | decision | Durable real-data fixture `tests/fixtures/skillopt_run/` = a 48K copy of the actual run (summary/history/best_skill/skills/2 real trajectories/results). Tests map it offline (no LLM). | Real data made durable beats a synthetic fixture; tests stay deterministic + cheap. |
| T041 | unknown | Honest run outcome: gate **rejected** the one proposed edit (best=seed); the baseline→best test delta 0.5→1.0 is 2-item sampling noise, not a real skill gain. | Tiny eval; the point is the substrate+loop ran for real, not a win. |
| T020 | deviation | **Contract change (user-approved, design A).** Recon of the real `gepa.optimize()` (from the pre-drop `03-aime-mini-live.ipynb` that produced `_gepa_run_07`) showed GEPA owns its own loop and exposes progress via `GEPACallback` — it does not surrender per-arrow control to our `run_iteration`. Revised loop-contract C-1 to two integration modes: *driver mode* (Optimizer Protocol + run_iteration, for simple optimizers) and *engine mode* (library owns the loop; adapter maps callback events onto substrate entities). GEPA = engine mode. SC-003's "no optimizer-specific harness code" reframed as "no optimizer-specific *substrate* code." | A mature optimizer library can't be driven arrow-by-arrow without reimplementing it; design A keeps the adapter thin and reuses the real engine. Spec claims (FR-001/003/005/007, SC-001/003) all still hold. |
