# docs/

Project documentation.

- `graduation.md` — the measurable thresholds (memory fan-out, latency floor,
  concurrency) at which the hot path is ported out of the live kernel to a
  compiled service, rather than wrapped in a headless batch executor (FR-011).
